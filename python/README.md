# Python example

This directory contains a minimal Python example that prints "Hello World".

Files
- `helloWorld.py` - Simple Python script printing a greeting.

Run
1. Run with Python 3:

   python helloWorld.py

Notes
- Use `python --version` to verify your Python installation.
